/* src/js/data/Library.js */

// 定义系统书籍内容
const GUIDE_BOOK_I = {
    id: "guide_book_part1",
    title: "伊萨卡手记 I：出发",
    cover: "assets/images/booksheet/booksheet0.png",
    date: "系统指南",
    isReadOnly: true, // 🔒 核心标记：只读
    content: `
# 序言

本手记旨在探讨一个核心命题：在现代性语境下，个体如何通过叙事行为重构自我认同。

---

## 第一章：叙事认同的理论图谱

我们需要首先处理本体论层面的问题：**“我”是谁？**

在分析哲学和现象学的视野中，**保罗·利科（Paul Ricoeur）**提供了一个关键的区分：作为“同一性”（Idem）的自我与作为“自身性”（Ipse）的自我。前者是生物学意义上的持久，而后者是承诺与责任的主体。利科认为，连接这两者的桥梁正是**叙事（Narrative）**。时间只有通过叙事被分节，才具有人类学的意义。

此观点在现代哲学中得到了广泛的回响：
* **麦金泰尔（Alasdair MacIntyre）** 在《德行之后》中论证，人类本质上是“讲故事的动物”，生活具有一种“叙事的统一性”。
* **查尔斯·泰勒（Charles Taylor）** 指出，自我是存在于道德空间中的导航，而叙事提供了方位的坐标。
* **安东尼·吉登斯（Anthony Giddens）** 则强调，自我认同并非既定事实，而是个体依据经历持续维持的一套连贯的“传记性叙事”。

综上所述，书写并非简单的记录，而是一种**本体论意义上的建构行为**。

## 第二章：原型回归——《奥德赛》细读

这种“叙事建构自我”的机制，并非现代发明。通过对荷马史诗《奥德赛》的文本细读，我们可以发现其中隐含的双重叙事策略。

奥德修斯的归乡（Nostos）实则是一场身份的重建。值得注意的是他在两个不同场域的叙事展演：

1.  **公共场域（费埃克斯宫殿）**：面对阿尔基诺奥斯王与满座宾客，奥德修斯通过宏大的口头演说，将海上漂泊的离散经验整合进“拉伊尔提斯之子”的英雄谱系中。这是 **社会身份（Social Identity）** 的确认。
2.  **私密场域（牧猪奴的茅舍）**：在重返宫殿前，他先在边缘的茅舍中通过伪装与低语，讲述了关于受难、忍耐与命运无常的故事。这是 **内在自我（Inner Self）** 的抚慰与整合。

这种“宫殿-茅舍”的二元结构，揭示了叙事功能的完整性：它既是个体向社会的投射，也是个体对内心的观照。

## 第三章：流动的现代性与锚点的丧失

然而，将古典经验移植到当代社会时，我们遭遇了**齐格蒙特·鲍曼（Zygmunt Bauman）** 所言的“流动的现代性”（Liquid Modernity）。

奥德修斯的幸运在于，他的“伊萨卡”是地理与社会结构中固定的锚点。但当代人的生存现实发生了结构性断裂：
* **宏大叙事的解体**：社会不再提供统一的意义剧本。
* **身份的液化**：职业、居住地、人际关系皆处于瞬息万变之中。

我们面临着一种深刻的“本体性不安全感”（Ontological Insecurity）。如果在外部世界找不到坚固的伊萨卡，个体便如同失去了坐标的航船。这正是当代焦虑的根源：**我们拥有讲述的欲望，却失去了讲述的参照系。**

## 第四章：作为解决方案的书写

面对流动的虚无，本研究项目（即这本《伊萨卡手记》）提出了一种基于“叙事疗法”的解决方案：**通过结构化的书写，在流动中重建锚点。**

如果外部的伊萨卡已不复存在，我们必须通过书写在内部将其重建。据此，我们将重建工程划分为两个阶段（即后续的分册内容）：

1.  **锚点一：叙事与自我（茅舍阶段）**
    对应《手记 II》。我们将首先在私密领域工作，通过对感知、记忆与情绪的微观书写，对抗时间的碎片化，重建内在的连贯性。

2.  **锚点二：叙事与社会（宫殿阶段）**
    对应《手记 III》。随后我们将进入公共视野，剖析“拟剧论”下的面具与角色，重新审视个体与社会主导叙事的关系，在喧嚣中确立主体性。

这不仅仅是一次写作练习，而是一场**存在的抵抗**。
请翻开下一页，握紧你的笔。这就是你的桨。
`
};

export const Library = {
    books: [],
    
   async init() {
        // 1. 读取存档
        const saved = await window.ithacaSystem.loadData('library_data.json');
        if (saved) {
            this.books = JSON.parse(saved);
        } else {
            this.books = [];
        }

        // --- 🧹 现有逻辑：清理旧的系统书 ---
        this.books = this.books.filter(b => {
            const isOldSystemBook = (b.title.includes("伊萨卡手记") && b.id !== GUIDE_BOOK_I.id && !b.isMystery);
            return !isOldSystemBook;
        });

        // --- 🛠️ 现有逻辑：注入/更新《伊萨卡手记 I》 ---
        const guideIndex = this.books.findIndex(b => b.id === GUIDE_BOOK_I.id);
        if (guideIndex === -1) {
            this.books.unshift(GUIDE_BOOK_I);
        } else {
            this.books[guideIndex] = { 
                ...this.books[guideIndex], 
                content: GUIDE_BOOK_I.content,
                isReadOnly: true,
                title: GUIDE_BOOK_I.title
            };
        }

        // ============================================================
        // ✨ 新增修复逻辑：强制更新《糖水菠萝的日记》的封面
        // ============================================================
        const targetBookId = "book_pineapple_diary_complete";
        const pineappleBook = this.books.find(b => b.id === targetBookId);
        
        if (pineappleBook) {
            // 强制覆盖为新的绿色封面 (booksheet1)
            pineappleBook.cover = "assets/images/booksheet/booksheet1.png"; 
            
            // 顺手再次确保它是只读的
            pineappleBook.isReadOnly = true; 
            
            console.log("已修复《糖水菠萝的日记》封面与属性");
        }

        // 3. 保存更改到硬盘
        this.save(); 
    },async init() {
        // 1. 读取存档
        const saved = await window.ithacaSystem.loadData('library_data.json');
        if (saved) {
            this.books = JSON.parse(saved);
        } else {
            this.books = [];
        }

        // --- 🧹 现有逻辑：清理旧的系统书 ---
        this.books = this.books.filter(b => {
            const isOldSystemBook = (b.title.includes("伊萨卡手记") && b.id !== GUIDE_BOOK_I.id && !b.isMystery);
            return !isOldSystemBook;
        });

        // --- 🛠️ 现有逻辑：注入/更新《伊萨卡手记 I》 ---
        const guideIndex = this.books.findIndex(b => b.id === GUIDE_BOOK_I.id);
        if (guideIndex === -1) {
            this.books.unshift(GUIDE_BOOK_I);
        } else {
            this.books[guideIndex] = { 
                ...this.books[guideIndex], 
                content: GUIDE_BOOK_I.content,
                isReadOnly: true,
                title: GUIDE_BOOK_I.title
            };
        }

        // ============================================================
        // ✨ 新增修复逻辑：强制更新《糖水菠萝的日记》的封面
        // ============================================================
        const targetBookId = "book_pineapple_diary_complete";
        const pineappleBook = this.books.find(b => b.id === targetBookId);
        
        if (pineappleBook) {
            // 强制覆盖为新的绿色封面 (booksheet1)
            pineappleBook.cover = "assets/images/booksheet/booksheet1.png"; 
            
            // 顺手再次确保它是只读的
            pineappleBook.isReadOnly = true; 
            
            console.log("已修复《糖水菠萝的日记》封面与属性");
        }

        // 3. 保存更改到硬盘
        this.save(); 
    },

    // 增
    addBook(book) {
        this.books.push(book);
        this.save();
    },

    // 特殊增加逻辑
    addMysteryBook(data) {
        const mysteryBook = {
            id: "mystery_book_01", // 这里如果以后有多本神秘书，建议用 uuid 或传入 ID
            title: data.title,
            author: data.author,
            content: data.content,
            cover: data.cover,
            isMystery: true,
            isCollected: true
        };
        
        if (!this.books.find(b => b.id === mysteryBook.id)) {
            this.books.unshift(mysteryBook);
            this.save();
        }
    },

    // 改 (合并后的版本)
    updateBook(id, title, content) {
        const book = this.books.find(b => b.id === id);
        if (book) {
            // 🔒 保护逻辑
            if (book.isReadOnly) {
                console.warn("试图修改只读书籍，操作被拦截");
                return false; // 返回 false 表示失败
            }
            book.title = title;
            book.content = content;
            this.save();
            return true; // 返回 true 表示成功
        }
        return false;
    },

    // 删 (合并后的版本，去掉了 deleteBook，统一用 removeBook)
    removeBook(id) {
        const book = this.books.find(b => b.id === id);
        
        // 🔒 保护逻辑
        if (book && book.isReadOnly) {
            console.warn(`书籍 ${book.title} 是系统书籍，无法销毁。`);
            return false; 
        }

        const initialLength = this.books.length;
        this.books = this.books.filter(b => b.id !== id);
        
        if (this.books.length !== initialLength) {
            this.save();
            return true;
        }
        return false;
    },

    // 查
    getAll() {
        return this.books;
    },

    // 存
    save() {
        window.ithacaSystem.saveData('library_data.json', JSON.stringify(this.books));
    }
};